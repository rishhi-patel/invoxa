@front = http://localhost:3000
@token = {{login.response.body.$.user}}  // placeholder (cookie will be set)

### Login via Next (sets cookie)
POST {{front}}/api/auth/login
Content-Type: application/json

{
  "email": "rishhi@example1.com",
  "password": "your-password-here"
}

### Clients (proxy) — List (uses cookie automatically)
GET {{front}}/api/clients

### Create client
POST {{front}}/api/clients
Content-Type: application/json

{
  "name": "Proxy Smoke Client",
  "email": "proxy.client@example.com",
  "phone": "555-0000",
  "company": "EXOcode Labs"
}

### Insights summary (proxy)
GET {{front}}/api/insights/summary

### Revenue trends
GET {{front}}/api/insights/revenue-trends?months=3

### Recent activity
GET {{front}}/api/insights/recent-activity?limit=5
